---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: persistentvolumescalers.pvc.scaler.opstreelabs.in
spec:
  group: pvc.scaler.opstreelabs.in
  names:
    kind: PersistentVolumeScaler
    listKind: PersistentVolumeScalerList
    plural: persistentvolumescalers
    singular: persistentvolumescaler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PersistentVolumeScaler is the Schema for the persistentvolumescalers
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PersistentVolumeScalerSpec defines the desired state of PersistentVolumeScaler
            properties:
              pvcRefName:
                items:
                  type: string
                type: array
              scaleParameters:
                description: ScaleParameters defines the scaling parameters for PersistentVolume
                properties:
                  scaleValuePercentage:
                    format: int32
                    type: integer
                  thresholdValuePercentage:
                    format: int32
                    type: integer
                required:
                - scaleValuePercentage
                - thresholdValuePercentage
                type: object
            required:
            - pvcRefName
            - scaleParameters
            type: object
          status:
            description: PersistentVolumeScalerStatus defines the observed state of
              PersistentVolumeScaler
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
